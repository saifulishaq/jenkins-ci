pipeline {
    agent any

    stages {
        stage('Image build'){
            steps{
                sh 'docker build -f ./Dockerfile-nightly-image -t my-playwright .'
                sh 'docker build -f ./Dockerfile-aws-cli -t my-aws-cli .'
            }
        }
    }
}